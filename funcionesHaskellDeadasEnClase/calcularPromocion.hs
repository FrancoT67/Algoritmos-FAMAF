--Se define funcion promedio con a y b como valores a evaluar.

promedio(a,b)=(a+b)/2

--Se define funcion promocion con A,B como valores a evaluar y C
--como el limite a evaluar.

promocion(a,b,c)= (a>=c && b>=c) && (promedio(a,b)>=7)

 